package oefeningen;
/*
import view.IO;
import database.QuizDatabase;
import model.*;
import java.util.*;

import java.util.ArrayList;
import java.util.HashSet;

public class Oef6c {

	
	public static void main(String[] args) {
		QuizDatabase quizDB = new QuizDatabase("quizen.txt");
		ArrayList <Quiz> quizen = quizDB.getQuizen();		
		
		TreeSet<Quiz> quizenTree = new TreeSet<Quiz>(new Oef6cQuizComparator());
		quizenTree.addAll(quizen);
		toonLijst(quizenTree);
		//Versie 2
		Collections.sort(quizen,new Oef6cQuizComparator());
		toonLijst(quizen);

	}
	
	public static void toonLijst(Collection<Quiz> quizen){
		String output ="";
		Iterator<Quiz> it = quizen.iterator();
		while (it.hasNext()){
			Quiz quiz = it.next();
			output += String.format("%3d  %-20s\n", quiz.getAantalOpdrachten(),quiz.getOnderwerp());
		}
		IO.toonStringMetVenster(output);
	}

}*/
